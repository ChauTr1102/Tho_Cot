# Copy this file to .env in this folder (QA_checklist/) and fill in your key.
#   cp .env.example .env
#
# Only needed for --live mode (real BytePlus ModelArk calls). Not needed for
# the default built-in generator mode.

ARK_API_KEY=ark-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
ARK_BASE_URL=https://ark.ap-southeast.bytepluses.com/api/v3
